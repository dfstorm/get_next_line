
#include "libft.h"
#include "get_next_line.h"

static int g_ipos;

char	*read_input(int fd, int *index)
{
	
	
	char	cbuffer[BUFF_SIZE];


	// 1- find line leight;
	// is there any 

	
	while ((i = read(fd, cbuffer, BUFF_SIZE)) > 0)
	{
		if (!check_pcs(buf))
			return (NULL);
		tetriminos[*index] = (buf, *index);
		(*index)++;

		retread = read(fd, buf, 1);

	}
	if (*buf == '\n' || retread != 0 || tot_read == 0)
		return (NULL);

	
	return (tetriminos);
}

int	get_next_line(const int fd, char **line)
{
	


	if(ipos > 0)
	if(fd || line)
	{
	}

	isafety = 1;



	ft_putstr("success");

	return (1);
}
